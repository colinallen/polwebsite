#!qtype ATT
#!preamble <font color="#ee0077" face="helvetica"><strong>Part X: </strong>
#!preamble Valid or Invalid?</font>&nbsp;&nbsp; 
#!preamble Use abbreviated truth tables to determine 
#!preamble whether or not the following arguments are valid.

#1
P -> (~Q . ~R), ~R <-> Q  :.  R v ~P
#2
~A v (B . C), C -> (B v A), ~B <-> ~C :. C -> A
